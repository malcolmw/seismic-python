.. _css3.1-pfseq_attributes:

**pfseq** -- pf sequence number
-------------------------------

This specifies an integer ordinal sequence number
for a particular parameter file object within a
sequence of objects with the same pfstringident.

* **Field width:** 6
* **Format:** %6ld
* **Null:** -1
* **Range:** seq >= 0
