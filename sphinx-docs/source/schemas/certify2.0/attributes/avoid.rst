.. _certify2.0-avoid_attributes:

**avoid** -- avoid tests on machines with
-----------------------------------------

Don't run test if machine has this characteristic:
marmot   		don't run on the marmot
Linux		don't run on linux

* **Field width:** 10
* **Format:** %-10s
